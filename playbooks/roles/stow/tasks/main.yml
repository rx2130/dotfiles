- name: Create symlinks
  command: stow shell vim
  args:
    chdir: ~/dotfiles

- name: Create Mac specific symlinks
  when: ansible_distribution == 'MacOSX'
  command: stow alacritty hammerspoon karabiner
  args:
    chdir: ~/dotfiles
